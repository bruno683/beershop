{% extends 'base.html.twig' %}

{% block title %}La boutique{% endblock %}

{% block body %}
<div class="shop_container">
    <h1>{{title}}</h1>
     {% for item in items %}
        <div class="card">
            {{item.name}}

        </div>
        
    {% endfor %} 
</div>
    
    
  


{% endblock %}
